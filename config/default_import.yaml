# Default configuration for data import step (gis_import task).
# Override in your user config under data_imports.
# See default_gis_importer.yaml for GIS-specific import settings.

data_imports:
    vectors: {}
    rasters: {}
